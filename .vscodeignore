# Source & tests — not needed at runtime
src/**
tests/**
scripts/**

# Docs & references
docs/**
references/
references/**
.cursor/**
*.plan.md
CLAUDE.md
AGENTS.md

# Dev configs
.husky/**
.lintstagedrc.json
.github/**
.vscode-test/**
esbuild.mjs
eslint.config.mjs
tsconfig*.json
.eslintrc*
.eslintignore
vitest.config*
biome.json
.editorconfig

# Build artifacts that aren't the final bundle
dist-types/**
dist/**/*.map
dist/test/**

# Node
node_modules/**

# Git
.git/**
.gitignore

# Local tooling / Cursor agent worktrees / IDE configs — never ship
.kilo/**
.claude/**
.vibe/**

# Misc
*.vsix
builds/**
.DS_Store
nul
cli/**

# DuckDB analytics bridge source — the feature is not part of 1.0.0 and the
# bundle is no longer built (see docs/v2/deferredWiring.md). Shipping the
# source of a disabled feature only inflates the package.
media/duckdb-bridge.src.js

# Donation QR — README links it by raw.githubusercontent URL, nothing reads the
# local copy. The file stays in git; it just has no reason to ship.
media/QR-Code.jpg

# Developer note about recording the walkthrough clips — the manifest wires up
# only the three markdown wrappers next to it.
media/walkthroughs/README.md
